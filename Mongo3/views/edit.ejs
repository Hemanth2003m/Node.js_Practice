<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the msg</title>
</head>
<body>
    <h3>Edit the msg</h3>
    <h4>this is the id <%= chat._id %></h4>
    <p>the msg send from <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form method="post" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" ><%= chat.msg %></textarea>
        <button>Re-send</button>
    </form>
    
</body>
</html>